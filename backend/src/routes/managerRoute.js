const express = require("express");
const router = express.Router();

const managerController = require("../controllers/managerController");
/* 
router.get("/", managerController.manager_produtos);

router.get("/inbox", managerController.manager_inbox);

router.post("/inbox/create", managerController.manager_message_create); */

